meta {
  name: Auth::Login
  type: http
  seq: 10
}

post {
  url: http://localhost:8080/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@mail.de",
    "password": "mypassword"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
